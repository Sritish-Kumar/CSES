# Sets

num=[i for i in range(int(input()))]

if sum(num)%2!=0:
    # odd
    print('Not possible')
else:
    # even

    req=sum(num)//2
    seta=[]

    # Try adding elements to make it to the required number. 

# not sure it will work there may be some even sum(num) which cant be divided

# ................. ):
